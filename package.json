{
  "name": "orbit",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "postinstall": "npm run build --workspace=@orbit/shared",
    "backend:dev": "npm run start:dev --workspace=@orbit/backend",
    "mobile:start": "npm run start --workspace=@orbit/mobile",
    "shared:build": "npm run build --workspace=@orbit/shared"
  }
}
